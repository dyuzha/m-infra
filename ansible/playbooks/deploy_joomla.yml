---
- name: Установка Joomla на целевой хост
  hosts: "{{ target_host | default('msk-1') }}"
  vars:
    joomla_dir: /opt/joomla
  tasks:
    - include_role:
        name: joomla
        tasks_from: main.yml
